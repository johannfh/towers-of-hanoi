from model.app_model import AppModel


class AppController:
    """
    ### `AppController`

    Manages the **Application State** `AppModel`.
    """

    def __init__(self, app_model: AppModel):
        pass
